// write a program to calculate library fine based on late days as follows

\#include <stdio.h>



int main() {

&nbsp;int days\_late;

&nbsp;float fine = 0.0;

&nbsp;printf("Enter the number of days the book is late: ");

&nbsp;scanf("%d", \&days\_late);

&nbsp;if (days\_late <= 0) {

&nbsp;    printf("No fine. Book returned on time on early.\\n");

&nbsp;} else if (days\_late <= 5) {

&nbsp;    fine = days\_late \* 2.0;

&nbsp;    printf("Fine: ₹%.2f\\n", fine);

&nbsp;} else if (days\_late <= 10) { // days 6 to 10

&nbsp;fine = (5 \* 2.0) + ((days\_late - 5) \* 4.0);

&nbsp;printf("Fine: ₹%.2f\\n", fine);

} else if (days\_late <= 30) { // days 11 to 30

fine = (5 \* 2.0) + (5 \* 4.0) + ((days\_late - 10) \* 6.0);

printf("Fine: ₹%.2f\\n", fine);

} else { // more than 30 days

printf("Membership Cancelled.\\n");

}

&nbsp;   return 0;

}

